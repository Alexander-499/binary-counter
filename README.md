# Binary Counter
Despite the name, this is a simple counter not only for binary, but also for octal, decimal and hexadecimal number formats (All display the same number at the same time). You can change the speed of the increment.
